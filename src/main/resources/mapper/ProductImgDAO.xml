<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hust.o2o.dao.ProductImgDAO">
    <sql id="table">tb_product_img</sql>
    <sql id="insertFields">img_addr,img_desc,priority,create_time,product_id</sql>
    <sql id="selectFields">
        product_img_id,<include refid="insertFields"/>
    </sql>


    <insert id="batchInsertProductImg" parameterType="list">
        INSERT INTO
        <include refid="table"/>
        (<include refid="insertFields"/>)
        VALUES
        <foreach collection="list" item="productImg" index="index" separator=",">
            (
            #{productImg.imgAddr},
            #{productImg.imgDesc},
            #{productImg.priority},
            #{productImg.createTime},
            #{productImg.productId}
            )
        </foreach>
    </insert>

</mapper>